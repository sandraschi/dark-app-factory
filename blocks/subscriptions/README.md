# Subscriptions Block

Subscription plans and feature flags — tier management, usage tracking, upgrade/downgrade flows, and plan comparison.

**Triggers**: subscription, plan, tier, pricing, member, upgrade, downgrade, feature.flag, billing

**Env vars**: (none)

**API endpoints**: `/api/subscriptions/plans`, `/api/subscriptions/plans/{id}`, `/api/subscriptions/user/{id}`

**Dependencies**: (none)
