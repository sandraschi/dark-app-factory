# Notify Block

Notification system — push, email, and SMS routing, notification templates, priority levels, and read tracking.

**Triggers**: notify, notification, alert, push, reminder, announcement

**Env vars**: (none)

**API endpoints**: `/api/notifications`, `/api/notifications/{id}/read`, `/api/notifications/send`

**Dependencies**: (none)
