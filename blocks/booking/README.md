# Booking Block

Calendar booking system — availability slots, appointment scheduling, reminders, and calendar sync.

**Triggers**: booking, appointment, calendar, schedule, reservation, slot, availability, book

**Env vars**: (none)

**API endpoints**: `/api/booking/slots`, `/api/booking/appointments`, `/api/booking/appointments/{id}`

**Dependencies**: (none)
