openai>=1.52.0
pydantic>=2.9.0
rich>=13.9.0
tenacity>=9.0.0
python-dotenv>=1.0.0
fastapi>=0.115.0
uvicorn>=0.30.0

# Test dependencies
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0

# Verification tools (optional -- install via uvx or uv tool install)
# showboat   # uvx showboat  (Go binary on PyPI)
# rodney     # uvx rodney    (Go binary on PyPI)
